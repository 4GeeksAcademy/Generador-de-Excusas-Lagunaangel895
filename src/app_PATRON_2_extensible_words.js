/**\n * PATRÓN EDUCATIVO #2: Función para Extender Palabras\n * \n * ¿QUÉ PROBLEMA RESUELVE?\n * Tu proyecto está bien, pero si quieres agregar más palabras,\n * necesitas editar el archivo. Con esta función, puedes agregar\n * palabras dinámicamente sin modificar el código.\n * \n * ¿QUÉ BENEFICIO TIENE?\n * - Aprendes a extender funcionalidad\n * - Entiendes cómo reutilizar arrays\n * - Practicas con condicionales (if/else)\n * \n * ¿ES OBLIGATORIO?\n * NO. Tu código actual funciona perfecto. Esto es una SUGERENCIA EDUCATIVA.\n * Úsalo si quieres practicar más.\n */\n\nconst who = ['El perro', 'Mi abuela', 'El cartero', 'Mi pájaro'];\nconst action = ['se comió', 'orinó sobre', 'aplastó', 'rompió'];\nconst what = ['mi tarea', 'mi teléfono', 'el carro'];\nconst when = ['antes de clase', 'mientras dormía', 'mientras hacía ejercicio', 'durante el almuerzo', 'mientras rezaba'];\n\n// ============================================\n// NUEVO: Función para Agregar Palabras\n// ============================================\n/**\n * Agrega una palabra a la categoría especificada\n * @param {string} category - La categoría (who, action, what, when)\n * @param {string} word - La palabra a agregar\n * \n * EJEMPLOS DE USO:\n * addWord('who', 'Mi gato')        → Agrega a who\n * addWord('action', 'mordió')      → Agrega a action\n * addWord('what', 'mis calcetines') → Agrega a what\n * addWord('when', 'anoche')        → Agrega a when\n */\nfunction addWord(category, word) {\n  // Verificamos qué categoría es y agregamos a la correcta\n  if (category === 'who') {\n    who.push(word);\n    console.log(`✅ Agregado \"${word}\" a la categoría \"who\"`);\n  } \n  else if (category === 'action') {\n    action.push(word);\n    console.log(`✅ Agregado \"${word}\" a la categoría \"action\"`);\n  } \n  else if (category === 'what') {\n    what.push(word);\n    console.log(`✅ Agregado \"${word}\" a la categoría \"what\"`);\n  } \n  else if (category === 'when') {\n    when.push(word);\n    console.log(`✅ Agregado \"${word}\" a la categoría \"when\"`);\n  } \n  else {\n    console.log(`❌ Categoría \"${category}\" no encontrada. Usa: who, action, what, when`);\n  }\n}\n\n// ============================================\n// TU CÓDIGO ACTUAL (sin cambios)\n// ============================================\n\nfunction generarExcusa() {\n  const sujeto = who[Math.floor(Math.random() * who.length)];\n  const verbo = action[Math.floor(Math.random() * action.length)];\n  const objeto = what[Math.floor(Math.random() * what.length)];\n  const momento = when[Math.floor(Math.random() * when.length)];\n  return `${sujeto} ${verbo} ${objeto} ${momento}.`;\n}\n\nfunction mostrarExcusa() {\n  const excusa = generarExcusa();\n  const elemento = document.getElementById(\"excuse\");\n  if (elemento) elemento.innerText = excusa;\n}\n\nfunction leerExcusa() {\n  const texto = document.getElementById(\"excuse\")?.innerText;\n  if (texto) {\n    const speech = new SpeechSynthesisUtterance(texto);\n    speech.lang = 'es-ES';\n    window.speechSynthesis.speak(speech);\n  }\n}\n\ndocument.addEventListener(\"DOMContentLoaded\", mostrarExcusa);\ndocument.getElementById(\"btnExcusa\")?.addEventListener(\"click\", mostrarExcusa);\ndocument.getElementById(\"btnLeer\")?.addEventListener(\"click\", leerExcusa);\n\n// ============================================\n// CÓMO USAR addWord (en la consola del navegador):\n// ============================================\n// addWord('who', 'Mi profesor');\n// addWord('action', 'robó');\n// addWord('what', 'mis apuntes');\n// mostrarExcusa(); // Genera nueva excusa con las nuevas palabras\n"}