/**\n * PATRÓN EDUCATIVO #1: Función Helper para Evitar Repetición\n * \n * ¿QUÉ PROBLEMA RESUELVE?\n * Tu código actual repite: Math.floor(Math.random() * array.length) cuatro veces\n * Esto hace el código más largo y difícil de mantener.\n * \n * ¿QUÉ BENEFICIO TIENE?\n * - Código más limpio y legible\n * - Si cambias la lógica, lo haces en UN solo lugar\n * - Aprendes a crear funciones reutilizables\n * \n * ¿ES OBLIGATORIO?\n * NO. Tu código actual funciona perfecto. Esto es una SUGERENCIA EDUCATIVA.\n * Úsalo cuando te sientas cómodo.\n */\n\nconst who = ['El perro', 'Mi abuela', 'El cartero', 'Mi pájaro'];\nconst action = ['se comió', 'orinó sobre', 'aplastó', 'rompió'];\nconst what = ['mi tarea', 'mi teléfono', 'el carro'];\nconst when = ['antes de clase', 'mientras dormía', 'mientras hacía ejercicio', 'durante el almuerzo', 'mientras rezaba'];\n\n// ============================================\n// NUEVO: Función Helper Reutilizable\n// ============================================\n/**\n * Selecciona un elemento aleatorio de un array\n * @param {array} array - El array del cual seleccionar\n * @returns {any} Un elemento aleatorio del array\n * \n * CÓMO FUNCIONA:\n * 1. Math.random() genera número entre 0 y 1\n * 2. Lo multiplica por array.length (ej: 0.5 * 4 = 2)\n * 3. Math.floor() redondea hacia abajo (2.7 → 2)\n * 4. Retorna array[2]\n */\nfunction getRandomElement(array) {\n  return array[Math.floor(Math.random() * array.length)];\n}\n\n// ============================================\n// GENERACIÓN DE EXCUSA - Versión Mejorada\n// ============================================\nfunction generarExcusa() {\n  // ANTES (tu forma - también correcta):\n  // const sujeto = who[Math.floor(Math.random() * who.length)];\n  // const verbo = action[Math.floor(Math.random() * action.length)];\n  // const objeto = what[Math.floor(Math.random() * what.length)];\n  // const momento = when[Math.floor(Math.random() * when.length)];\n  \n  // AHORA (usando el helper - más limpio):\n  const sujeto = getRandomElement(who);\n  const verbo = getRandomElement(action);\n  const objeto = getRandomElement(what);\n  const momento = getRandomElement(when);\n  \n  return `${sujeto} ${verbo} ${objeto} ${momento}.`;\n}\n\nfunction mostrarExcusa() {\n  const excusa = generarExcusa();\n  const elemento = document.getElementById(\"excuse\");\n  if (elemento) elemento.innerText = excusa;\n}\n\nfunction leerExcusa() {\n  const texto = document.getElementById(\"excuse\")?.innerText;\n  if (texto) {\n    const speech = new SpeechSynthesisUtterance(texto);\n    speech.lang = 'es-ES';\n    window.speechSynthesis.speak(speech);\n  }\n}\n\ndocument.addEventListener(\"DOMContentLoaded\", mostrarExcusa);\ndocument.getElementById(\"btnExcusa\")?.addEventListener(\"click\", mostrarExcusa);\ndocument.getElementById(\"btnLeer\")?.addEventListener(\"click\", leerExcusa);\n"}